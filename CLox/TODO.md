# Arrays and Maps

- Make Hash Table available with native classes (syntax like { 1, 2, 3 }, like arrays but as hash maps).
- Arrays
    - contains method
    - Index Access syntax []
- foreach with arrays and maps
- hash table for Obj Types other than Strings

# Other

- named arguments
- Enums (integers but with metadata)
- Index access methods self declaration in class, like C# allows it

# DNet Framwork

Basicly allows Http in Lox.

- Client to send requests
- Server, use a class as the server, and methods are the Endpoints.
